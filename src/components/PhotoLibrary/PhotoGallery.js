// @flow

import * as React from "react";
import { Text } from "react-native";
import ViewWithFooter from "../SharedComponents/ViewWithFooter";

const PhotoGallery = ( ): React.Node => (
  <ViewWithFooter>
    <Text>photo gallery</Text>
  </ViewWithFooter>
);

export default PhotoGallery;
